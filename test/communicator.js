var home = require("../lib/phone-home")
	, machine = require('../machine');//machine.json


exports["Communicator"] = function(test){
    test.expect(1);
    test.ok(true, "this assertion should pass");
    test.done();
};
